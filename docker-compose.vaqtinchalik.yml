# Temporary HTTPS while surxan-paxta.uz is not active yet:
#   docker compose -f docker-compose.yml -f docker-compose.vaqtinchalik.yml up -d
# Generated by tools/vaqtinchalik_https.sh (Caddyfile.vaqtinchalik holds the password hash; not in git).
services:
  caddy:
    volumes:
      - ./Caddyfile.vaqtinchalik:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
